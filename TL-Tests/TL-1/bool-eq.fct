initialise-binding initialise-storing finalise-failing scope
  (collateral( ),
   print
     (to-string is-eq
        (not false,
         is-less
           (decimal "7",
            decimal "0")),
      "
"))