initialise-binding finalise-failing sequential
  (if-else
     (false,
      true,
      if-else
        (is-greater
           (decimal-natural "5",
            decimal-natural "4"),
         true,
         false)))