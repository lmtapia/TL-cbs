initialise-binding finalise-failing handle-return sequential
  (if-else
     (true,
      if-else
        (false,
         true,
         if-else
           (true,
            false,
            false)),
      false))