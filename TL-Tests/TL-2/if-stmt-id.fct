initialise-binding finalise-failing scope
  (collateral
     (bind
        ("a",
         allocate-initialised-variable
           (integers,
            0)),
      bind
        ("b",
         allocate-initialised-variable
           (integers,
            0))),
   sequential
     (assign
        (bound
           ("a"),
         decimal "5"),
      if-else
        (is-greater
           (assigned bound
              "a",
            decimal "0"),
         print
           (to-string assigned bound
              "a",
            "
"),
         print
           (to-string assigned bound
              "b",
            "
"))))