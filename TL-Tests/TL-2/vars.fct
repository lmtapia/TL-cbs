initialise-binding finalise-failing scope
  (collateral
     (bind
        ("x",
         allocate-initialised-variable
           (integers,
            0)),
      bind
        ("y",
         allocate-initialised-variable
           (integers,
            0))),
   handle-return sequential
     (null-value,
      assign
        (bound
           ("y"),
         decimal-natural "9")))