initialise-binding finalise-failing scope
  (collateral
     (bind
        ("a",
         allocate-initialised-variable
           (integers,
            0))),
   sequential
     (assign
        (bound
           ("a"),
         decimal "5"),
      print
        (to-string assigned bound
           "a",
         "
")))